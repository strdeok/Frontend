# WINGS Frontend 트러블슈팅

[← README로 돌아가기](./README.md)

## 1. 팝업 부스에서 로그인 요구가 대기열과 이탈을 만듦

### 문제

일반 서비스의 인증 흐름을 그대로 사용하면 팝업 부스 방문자는 가입·로그인 과정에서 멈추고, 짧은 체험 시간 안에 진단 결과까지 도달하기 어려웠습니다.

### 해결

게스트 전용 흐름을 분리했습니다. 로그인 없이 온보딩 → 사진 선택 → 분석 → 보정 설문 → 결과 → 추천 상품으로 이동할 수 있도록 하고, 결과는 현재 브라우저 세션에서만 유지하도록 제한했습니다. 게스트 진단 기록은 `user_id = null`, `requester_type = guest`로 구분하며 사진 원본은 영구 저장하지 않도록 설계했습니다.

### 결과

가입을 요구하지 않으면서도 부스 운영에 필요한 진단 흐름을 유지했고, 계정 기반 이력·찜 기능과 게스트 체험의 데이터 경계를 명확히 했습니다.

---

## 2. 로컬에서 Supabase 환경 변수가 없으면 화면 확인도 막힘

### 문제

UI 작업이나 부스 화면 검토 중에는 Supabase URL·키가 준비되지 않은 로컬 환경에서도 화면을 빠르게 확인할 수 있어야 합니다. 환경 변수가 없다는 이유로 앱 초기화나 라우팅이 실패하면 개발·운영 검토가 지연됩니다.

### 해결

Supabase가 설정되지 않은 경우를 로컬 미리보기 모드로 분리했습니다. 이 모드에서는 계정·영구 저장 요청을 만들지 않고 UI를 렌더링하며, `/preview`에서 부스·사용자·관리자 화면 경로를 빠르게 확인할 수 있게 구성했습니다.

### 결과

외부 서비스 준비 상태와 무관하게 디자인·동선 검토를 진행할 수 있고, 실제 데이터가 없는 환경에서 잘못된 저장 요청이 발생하지 않습니다.

---

## 3. AI 키와 민감한 입력값을 클라이언트에 노출할 위험

### 문제

사주 해석에는 Gemini API 키가 필요하고, 입력값에는 생년월일·출생 시각 같은 민감한 정보가 포함됩니다. 클라이언트 코드나 브라우저 저장소에 이를 두면 키 노출과 불필요한 데이터 보관 위험이 생깁니다.

### 해결

사주 계산과 해석을 `POST /api/sazu` Vercel Function으로 분리했습니다. `GEMINI_API_KEY`는 서버 환경 변수에서만 읽고, 유효하지 않은 입력은 계산 전에 거절합니다. 출생 정보·계산 결과·해석 응답은 DB와 브라우저 저장소에 영구 보관하지 않으며, 실패 시에도 키·입력 원문·외부 응답을 사용자 메시지나 로그에 노출하지 않도록 했습니다.

### 결과

API 키의 실행 경계를 서버로 제한하고, 기능 구현과 개인정보 최소 보관 원칙을 함께 만족시켰습니다.

---

## 4. 사진 품질이 낮으면 진단 신뢰도가 떨어짐

### 문제

얼굴이 감지되지 않거나 여러 명이 포함된 사진, 지나치게 어두운 사진은 진단 결과의 신뢰도를 낮춥니다.

### 해결

MediaPipe FaceDetector로 얼굴 수와 얼굴 영역 비율을 확인하고, 평균 밝기 검증을 추가했습니다. 조건에 맞지 않는 이미지는 분석 전에 사용자에게 다시 촬영·선택하도록 안내합니다.

### 결과

분석 요청 전에 입력 품질을 한 번 검증해, 사용자가 이유를 알 수 없는 결과를 받는 상황을 줄였습니다.
